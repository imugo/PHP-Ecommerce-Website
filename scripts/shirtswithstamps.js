/*var MyApp = {};

// Shopping Cart dropdown funcionality
 function cartDropdownFunction() {
	document.getElementById("cart-dropdown-menu").classList.toggle("show");
}
console.log("hello");
// Close the window if the user clicks outside of it
window.onclick = function (event) {
	if (!event.target.matches(".dropbtn")) {
		var dropdowns = document.getElementsByClassName("cart-dropdown-content");
		var i;
		for (i = 0; i<dropdowns.length; i++) {
			var openDropdown = dropdowns[i];
			if (openDropdown.classList.contains("show")) {
				openDropdown.classList.remove("show");
			}
		}
	}
};
//const dropBotton = document.querySelectorAll("dropbtn");
//dropBotton[0].addEventListener("click", MyApp.cartDropdownFunction);*/
